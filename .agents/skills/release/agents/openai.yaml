interface:
  display_name: "Release"
  short_description: "Plan and publish a GitHub release"
  default_prompt: "Use $release to prepare a GitHub Release for this repo, propose the next vX.Y.Z or vX.Y.Z-beta.N tag, update CHANGELOG.md, and publish from the exact changelog commit."
